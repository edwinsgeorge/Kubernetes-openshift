apiVersion: v1
kind: Service
metadata:
  name: ml-routing
  namespace: emergency-call-center
  labels:
    app: ml-routing
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: ml-routing
