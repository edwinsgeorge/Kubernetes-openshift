apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  namespace: emergency-call-center
  labels:
    app: admin-dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: admin-dashboard
