apiVersion: v1
kind: Service
metadata:
  name: voice-backend
  namespace: emergency-call-center
  labels:
    app: voice-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: voice-backend
