apiVersion: v1
kind: Service
metadata:
  name: signaling-server
  namespace: emergency-call-center
  labels:
    app: signaling-server
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: signaling-server
